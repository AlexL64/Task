<Window x:Class="Task.View.ShowTask"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task.View"
        mc:Ignorable="d"
        Title="ShowTask" Height="450" Width="800">
    <Grid DataContext="{Binding TaskViewModel.Task}">
        <Grid.RowDefinitions>
            <RowDefinition Height="55*" />
            <RowDefinition Height="45*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <TextBox Background="#F00" Name="Title" Margin="10,10,10,10" Height="48" VerticalAlignment="Top" TextAlignment="Center" FontSize="20" VerticalContentAlignment="Center" Text="{Binding Path=title}"></TextBox>
            <TextBox Background="#0F0" Name="Description" Margin="10,68,10,10" Text="{Binding Path=description}"></TextBox>
        </Grid>
        <Grid Grid.Row="1" Background="#00F">
        </Grid>
    </Grid>
</Window>
